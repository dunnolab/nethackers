# Python
__pycache__/
*.py[cod]
*.egg
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / lint / type caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Environments & secrets (held-out HMAC key, deploy config — never committed)
.env
*.env
!*.env.example

# Per-worktree local-stack allocation (scripts/stack.py) — generated, host-specific
.env.stack
.nethackers/

# Implementation plans are working docs, not tracked artifacts
docs/superpowers/plans/

# Research scratch (raw eval-cost data + bench scripts; provenance, not tracked)
docs/superpowers/research/data/

# SDD scratch (ledger, briefs, reports, review packages)
.superpowers/

# Optional background track (The Nethack Dictionary) — shipped via deploy, never committed
src/nethackers/hub/web/dictionary.mp3

# OS
.DS_Store
